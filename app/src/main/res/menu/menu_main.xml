<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="Search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="always|collapseActionView" />
    <item
        android:id="@+id/action_search_range"
        android:title="@string/search_range">
        <menu android:id="@+id/search_range_group">
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/search_500_meters"
                    android:title="@string/search_500_meters" />
                <item
                    android:id="@+id/search_1000_meters"
                    android:title="@string/search_1000_meters" />
                <item
                    android:id="@+id/search_1500_meters"
                    android:title="@string/search_1500_meters" />
            </group>
        </menu>
    </item>
</menu>